@{
    ViewData["Title"] = "成為達人";
    Layout = "_Layout";
}
@using System.Security.Claims
@{
    var uid = User.FindFirstValue(ClaimTypes.NameIdentifier) ?? "";
}

@using System.Security.Claims
@{
    ViewData["Title"] = "小老闆模式";
    Layout = "_Layout";

    var roles = User.Claims
        .Where(c => c.Type == ClaimTypes.Role)
        .Select(c => c.Value)
        .ToList();

    bool isBoss = roles.Any(r => r.Contains("BOSS", StringComparison.OrdinalIgnoreCase));

    Console.WriteLine($"User Roles: {string.Join(", ", roles)}");
}

<div class="container mt-4">
    @if (!isBoss)
    {
        <!-- 未成為小老闆：顯示註冊問卷 -->
        @await Html.PartialAsync("_BossRegisterPartial")
    }
    else
    {
        <!-- 已是小老闆：顯示服務資訊 -->
        @await Html.PartialAsync("_BossDashboardPartial")
    }
</div>


<!-- Quill 核心 -->
<link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
<script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
<!-- Image Resize 模組 -->
<script src="https://cdn.jsdelivr.net/npm/quill-image-resize-module@3.0.0/image-resize.min.js"></script>
</div>
@* 
<script>
    // 初始化 Quill
    var quill = new Quill('#editor-container', {
      theme: 'snow',
      modules: {
        toolbar: [
          [{ 'size': ['small', false, 'large', 'huge'] }], // 新增這行
          ['bold', 'italic', 'underline'],
          [{ 'list': 'ordered'}, { 'list': 'bullet' }],
          ['link', 'image']
        ],
        imageResize: {
          modules: [ 'Resize', 'DisplaySize', 'Toolbar' ]
        }
      }
    });

    // TODO: 從登入 Cookie 或後端回傳取 userId / storeId
    const userId = document.getElementById("currentUserId")?.value;

    const storeId = "11111111-1111-1111-1111-111111111111";

    // 自動載入使用者的內容
     document.addEventListener("DOMContentLoaded", async () => {
       const userId = document.getElementById("currentUserId")?.value;
       if (!userId) return;

    let res = await fetch(`https://localhost:7030/api/bossstorecontent/${userId}`);
    if (res.ok) {
      let data = await res.json();
      if (data && data.contentHtml) {
        quill.root.innerHTML = data.contentHtml;
      }
    }
     });

     // 儲存
     async function saveContent() {
       const userId = document.getElementById("currentUserId")?.value;
       if (!userId) {
         alert("尚未登入，無法儲存內容");
         return;
       }

       let html = quill.root.innerHTML;

    let res = await fetch(`https://localhost:7030/api/bossstorecontent/${userId}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ contentHtml: html })
    });

       if (res.ok) {
         alert("自定義內容已儲存");
       } else {
         alert("儲存失敗");
       }
     }
</script> *@